@model GymSystem.Web.Areas.Trainer.ViewModels.TrainerSessionViewModel

@{
    ViewData["Title"] = "Sessions";
    DateTime? currentDate = null;
}

<section class="schedule-page-wrapper">
    <div class="schedule-page-card">
        <h2>All Sessions</h2>

        <form method="get" asp-area="Trainer" asp-controller="Session" asp-action="Index" style="display: flex; gap: 0.5rem; margin-bottom: 1rem;">
            <input type="text" name="search" value="@Model.Search" placeholder="Search by subject or room..."
                   class="schedule-input" style="flex: 1; padding: 0.5rem 0.75rem; border-radius: 6px; border: 1px solid rgba(255,255,255,0.2); background: rgba(255,255,255,0.05); color: inherit;" />
            <button type="submit" class="schedule-btn">Search</button>
            @if (!string.IsNullOrWhiteSpace(Model.Search))
            {
                <a asp-area="Trainer" asp-controller="Session" asp-action="Index" class="schedule-btn" style="text-decoration: none;">Clear</a>
            }
        </form>

        <div class="detail-actions" style="margin-bottom: 1rem;">
            <a asp-area="Trainer" asp-controller="Session" asp-action="Create" class="schedule-btn">Create Session</a>
        </div>

        @if (Model.WeeklySessions != null && Model.WeeklySessions.Any())
        {
            @foreach (var session in Model.WeeklySessions)
            {
                if (currentDate == null || currentDate.Value.Date != session.Start.Date)
                {
                    currentDate = session.Start.Date;

                    <h3 class="schedule-date">@session.Start.ToString("dddd, dd MMMM yyyy")</h3>
                }

                <a asp-area="Trainer" asp-controller="Session" asp-action="Details" asp-route-id="@session.SessionId" class="schedule-row session-link">
                    <span>@session.Subject</span>
                    <span>
                        @session.Start.ToString("hh:mm tt") - @session.End.ToString("hh:mm tt")
                    </span>
                    <span>Room @session.RoomNumber</span>
                </a>
            }

            @if (Model.TotalPages > 1)
            {
                <nav class="pagination">
                    @if (Model.HasPrevious)
                    {
                        <a asp-area="Trainer" asp-controller="Session" asp-action="Index"
                           asp-route-page="@(Model.Page - 1)" asp-route-search="@Model.Search" class="page-link">&laquo; Previous</a>
                    }
                    else
                    {
                        <span class="page-link disabled">&laquo; Previous</span>
                    }

                    @for (int i = 1; i <= Model.TotalPages; i++)
                    {
                        if (i == Model.Page)
                        {
                            <span class="page-link active">@i</span>
                        }
                        else
                        {
                            <a asp-area="Trainer" asp-controller="Session" asp-action="Index"
                               asp-route-page="@i" asp-route-search="@Model.Search" class="page-link">@i</a>
                        }
                    }

                    @if (Model.HasNext)
                    {
                        <a asp-area="Trainer" asp-controller="Session" asp-action="Index"
                           asp-route-page="@(Model.Page + 1)" asp-route-search="@Model.Search" class="page-link">Next &raquo;</a>
                    }
                    else
                    {
                        <span class="page-link disabled">Next &raquo;</span>
                    }
                </nav>
            }
        }
        else
        {
            <div class="schedule-row">
                <span>No sessions found</span>
            </div>
        }
    </div>
</section>